#include <stdio.h>
int main (){
    int a ;
    printf("Enter the value for a \n");
    scanf("%d",&a);
    // one linear
    (a>0)? printf("a is natural number") : printf("a is not natural number");
    return 0;
}